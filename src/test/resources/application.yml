spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#      driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

### test폴더에서는 test 폴더 안에 있는 application.yml 파일이 우선적으로 적용됨
### 위의 설정들이 별도로 없으면, 기본이 메모리 모드로 설정됨

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
